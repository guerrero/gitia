package cli

// PreparseFixes rewrites bare numeric arguments that follow --fixes/-f into the
// comma-separated form pflag can parse. Task 13 implements the real logic.
func PreparseFixes(args []string) []string { return args }
